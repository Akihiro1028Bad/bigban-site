import { render, screen, waitFor } from "@testing-library/react";
import { afterEach, describe, expect, it, vi } from "vitest";
import { analyticsSnapshot } from "../../../../scripts/growth/__fixtures__/analyticsSnapshot";
import { AnalyticsClient } from "./AnalyticsClient";
describe("AnalyticsClient", () => { afterEach(() => vi.unstubAllGlobals()); it("初回は空状態を表示する", async () => { vi.stubGlobal("fetch", vi.fn(async () => new Response(JSON.stringify({ success: true, snapshot: null }), { status: 200 }))); render(<AnalyticsClient />); await waitFor(() => expect(screen.getByText("まだ取り込みがありません。npm run growth:ingest を実行してください。")).toBeVisible()); }); it("取得済みボードを描画する", async () => { vi.stubGlobal("fetch", vi.fn(async () => new Response(JSON.stringify({ success: true, snapshot: analyticsSnapshot() }), { status: 200 }))); render(<AnalyticsClient />); await waitFor(() => expect(screen.getByRole("heading", { name: "経営サマリー" })).toBeVisible()); }); it("通信失敗時は取得エラーを表示する", async () => { vi.stubGlobal("fetch", vi.fn(async () => { throw new Error("ネットワークに接続できません"); })); render(<AnalyticsClient />); await waitFor(() => expect(screen.getByRole("alert")).toHaveTextContent("ネットワークに接続できません")); }); it("Error以外の通信失敗時は既定メッセージを表示する", async () => { vi.stubGlobal("fetch", vi.fn(async () => { throw "通信失敗"; })); render(<AnalyticsClient />); await waitFor(() => expect(screen.getByRole("alert")).toHaveTextContent("経営ボードの取得に失敗しました。")); }); it("HTTP非OK時はレスポンスのエラーを表示する", async () => { vi.stubGlobal("fetch", vi.fn(async () => new Response(JSON.stringify({ success: true, error: "認証が必要です" }), { status: 401 }))); render(<AnalyticsClient />); await waitFor(() => expect(screen.getByRole("alert")).toHaveTextContent("認証が必要です")); }); it("successがfalseでエラーがない場合は既定メッセージを表示する", async () => { vi.stubGlobal("fetch", vi.fn(async () => new Response(JSON.stringify({ success: false }), { status: 200 }))); render(<AnalyticsClient />); await waitFor(() => expect(screen.getByRole("alert")).toHaveTextContent("経営ボードの取得に失敗しました。")); }); });
